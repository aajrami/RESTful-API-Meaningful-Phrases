# RESTful-API-Meaningful-Phrases
A simple project to build an API to extract the meaningful phrases (bigrams and trigrams) from a given text.

This project was implemented using:
- Python Flask
- Flask RESTful
- NLTK

